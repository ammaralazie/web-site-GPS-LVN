import Vuex from 'vuex';

const store = () => {
  return new Vuex.Store({
    state: {
      // Your state properties
    },
    mutations: {
      // Your mutations to update state
    },
    actions: {
      // Your actions to commit mutations
    },
    getters: {
      // Your getters to retrieve state
    }
  });
};

export default store;
